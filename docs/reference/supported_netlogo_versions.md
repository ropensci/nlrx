# Report supported NetLogo versions

Auxiliary function to report supported NetLogo version numbers

## Usage

``` r
supported_netlogo_versions()
```

## Value

a vector of supported Versions for Download and Usage (parameter
`version`):

## Examples

``` r
if (FALSE) { # \dontrun{
supported_netlogo_versions()
} # }
```
